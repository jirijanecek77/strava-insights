{$DOMAIN} {
    encode zstd gzip

    tls {$ACME_EMAIL}

    @api path /health /dashboard /trends /comparisons /me* /sync* /activities* /best-efforts* /auth*
    handle @api {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:80
    }
}
